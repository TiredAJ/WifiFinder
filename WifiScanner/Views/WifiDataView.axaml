<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:vm="using:WifiScanner.ViewModels"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="WifiScanner.Views.WifiDataView"
			 x:DataType="vm:WifiInfoViewModel">

		<DataGrid DockPanel.Dock="Bottom" ItemsSource="{Binding WifiItems}" AutoGenerateColumns="True"
				  IsReadOnly="True" GridLinesVisibility="All" BorderThickness="2"
				  BorderBrush="Gray" x:Name="dgrd_Data">
		</DataGrid>
		

		<!--
		<ListBox DockPanel.Dock="Top" ItemsSource="{Binding WifiItems}">
			
		</ListBox>
		<TextBlock DockPanel.Dock="Bottom" Text="{Binding Temp}"/>
		<TextBlock DockPanel.Dock="Right" Text="Right"/>
		-->
	
</UserControl>
